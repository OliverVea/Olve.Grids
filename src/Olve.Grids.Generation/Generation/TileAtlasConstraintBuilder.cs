using DeBroglie.Constraints;

namespace Olve.Grids.Generation.Generation;

public class TileAtlasConstraintBuilder
{
    public IReadOnlyCollection<ITileConstraint> BuildConstraints(TileAtlas.TileAtlas tileAtlas)
    {
        var constraints = new List<ITileConstraint>();

        return constraints;
    }
}